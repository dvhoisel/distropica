server {
    listen 80;
    listen [::]:80;
    server_name distropica.com.br;

    root /var/www/distropica.com.br;
    index index.html;

    location ^~ /.well-known/acme-challenge/ {
        try_files $uri =404;
    }

    location / {
        try_files $uri $uri/ =404;
    }
}
